version: 2

mkdocs:
  configuration: mkdocs.yml

build:
  os: "ubuntu-22.04"
  tools:
    python: "3.12"

  jobs:
    pre_build:
      # Comando para garantir que o MkDocs e o tema Material sejam instalados
      - pip install mkdocs mkdocs-material